export function BottomButton({ text }: { text: string }) {
    return (
        <button className="bg-boost text-white font-bold py-2 px-4 rounded-[20px] text-button font-jakarta tracking-stats">
            {text}
        </button>
    );
}
